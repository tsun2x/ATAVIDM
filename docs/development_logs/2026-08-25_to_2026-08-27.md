# Development Log

**Project:** TAVIDM

**Development Period:** 2026-08-25 to 2026-08-27

**Repository:** tavidm

**Primary Branch:** cursor/phase-1-foundation

**Last Updated:** 2026-08-25 03:30

---

# Overview

This document records all completed development work performed during this three-day development period.

Every log entry represents completed work only.

The documentation should always match the current repository state.

Do not document speculative work, future plans, or partially reverted implementations.

---

# Development Entries

---

## [2026-08-25 01:16] Feature: Uploaded-video processing UX (playback, live preview, annotated output, history, bulk, analytics)

### Summary

Delivered the complete uploaded-video processing experience: authenticated source/annotated media with HTTP Range support, one FIFO processing job with optional Live Processing Preview, rich monotonic status contract, incremental annotated MP4 artifacts, per-video history, safe remove-results/delete, bulk sequential enqueue, accessible green/red violation switches, and upload/processing analytics.

### Files Created

- `core/annotated_writer.py` — incremental annotated VideoWriter with temp→finalize
- `core/frame_annotate.py` — shared boxes/labels/zone/violation drawing
- `core/media_serve.py` — Range/conditional authenticated media responses
- `core/processing_progress.py` — monotonic ProgressTracker + status contract
- `core/processing_preview.py` — latest-JPEG hub (drop stale; viewer cannot slow job)
- `core/video_lifecycle.py` — remove-results + permanent delete with path guards
- `core/upload_analytics.py` — today/week/month/custom analytics (upload time only)
- `database/migrations/004_upload_processing_ux.sql` — additive progress/history schema
- `static/js/violation_switch.js` — reusable accessible sliding switches
- `tests/test_upload_processing_ux.py` — focused UX/media/progress/bulk/delete tests

### Files Modified

- `app.py` — media/history/bulk/delete/analytics routes; worker progress; status contract
- `config.py` — `ANNOTATED_FOLDER`, `APP_TIMEZONE`
- `core/video_processor.py` — annotated output, preview publish, progress stages
- `database/schema.sql` — processing_runs UX columns; history + audit tables; uploaded_by
- `database/sqlite_adapter.py` — migration 004; run progress; history; delete helpers; analytics
- `templates/live_monitor.html` — source/reference/live/annotated panes; progress; actions; bulk
- `templates/settings.html` — violation sliding switches
- `templates/analytics.html` — upload/processing analytics panel
- `static/js/live_monitor.js` — playback modes, progress UI, history/delete/bulk
- `static/js/upload.js` — data-video JSON list rows; post-upload stages
- `static/css/style.css` — switch + media-mode styles

### Implementation Details

- Single FIFO worker unchanged; watch-live only attaches MJPEG preview to the same run.
- Annotated artifacts use per-run dirs under `dataset/annotated/run_{id}/` with partial rename on success.
- Status API exposes run_id, stage, frames, percent, ETA (only when calculable), class_counts, annotated URLs.
- Delete/remove blocked while queued/processing and when confirmed violations/reports exist (HTTP 409).
- Analytics uses `videos.created_at` (app-local) for unique uploads; reprocessing increments run counts only.

### Testing Performed

- `python -m pytest tests/test_upload_processing_ux.py -v` — 19 passed
- `python -m pytest tests/ --tb=line` — 229 passed
- `git diff --check` on in-scope paths — clean (CRLF warnings only on unrelated files)

### Project Progress

- Uploaded-video UX now matches the processing pipeline’s actual detection volume (raw detections + candidates), not only Review Queue emptiness.

### Next Steps

- Manual browser pass: seek on source video, watch-live MJPEG, annotated download, bulk select, admin delete modal.
- Optional: serve admin DELETE as JSON 403 instead of HTML redirect for API clients.

---

## [2026-08-25 01:53] Correction: Uploaded-video processing UX — seven Codex findings

### Summary

Addressed the seven verified correction findings: failure-safe permanent delete with required confirmation, browser-playable WebM annotated output, processing_run_id attribution for reprocessing, Select All + atomic enqueue, viewer-gated preview JPEG encoding, upload/history/analytics/review-queue UI fixes, and gitignore for runtime annotated outputs.

### Files Created

- `database/migrations/005_processing_run_attribution.sql` — forward-only run attribution migration notes
- `dataset/annotated/.gitkeep` — keep annotated directory without tracking runtime artifacts

### Files Modified

- `core/video_lifecycle.py` — nonempty filename confirmation; quarantine-then-DB delete with restore on failure
- `core/annotated_writer.py` — prefer VP8/VP9 WebM; refuse AVI-as-MP4; MIME helpers
- `core/processing_preview.py` — viewer-count gate + ~5 FPS wall-clock throttle before JPEG encode
- `core/video_processor.py` — write detections/reviews with processing_run_id; gated preview publish
- `database/schema.sql` — processing_run_id on detections/review_queue/violations + indexes
- `database/sqlite_adapter.py` — migration 005; run-scoped detection summary; inserts carry run id
- `app.py` — atomic enqueue; annotated MIME/download name; history enrichment; DELETE confirmation
- `static/js/live_monitor.js` — Select All outside list; history fields; /review-queue link; escape text
- `static/js/upload.js` — preparing / uploading-saving / extracting phase order
- `templates/analytics.html` — today/week/month trend chart for uploads vs processing runs
- `.gitignore` — ignore dataset/annotated/* except .gitkeep; quarantine/partials
- `tests/test_upload_processing_ux.py` — correction-pass coverage for all seven findings
- `docs/development_logs/2026-08-25_to_2026-08-27.md` — this entry

### Implementation Details

1. Permanent delete requires exact nonempty filename match in route and service; files are quarantined before DB cascade and restored if DB fails.
2. Annotated writer uses OpenCV-bundled VP8 WebM (no new FFmpeg/OpenH264 install). Extension, MIME, and download name agree.
3. Current detection summaries use the latest completed run's attributed rows; failed runs do not replace prior completed results. Legacy NULL run ids remain readable until a new attributed run completes.
4. bulkSelectAll listener attached outside #videoList; eligible-only selection + indeterminate state; enqueue check+claim under queue lock.
5. Preview JPEG encode skipped when viewer_count==0; multi-viewer shares one encode; ~5 FPS display cap.
6. Upload stages truthful during XHR; history shows rules/timestamps/Unknown recorded_at; analytics renders trend series; Open Review Queue → /review-queue.
7. Runtime annotated outputs gitignored; .gitkeep preserved.

### Testing Performed

- `python -m pytest tests/test_upload_processing_ux.py -q --tb=short` — 34 passed
- `python -m pytest tests/ -q --tb=line` — 244 passed
- `git diff --check` — exit 0 (CRLF warnings only)
- Browser (Cursor IDE browser / Chromium): AnnotatedVideoWriter VP8 WebM — readyState=4, 640×360, duration=4s, currentTime advanced, seeked events fired
- `git check-ignore` confirms `dataset/annotated/run_*/` ignored and `.gitkeep` negated

### Project Progress

- Correction pass ready for independent Codex re-review on dirty branch `cursor/phase-1-foundation` @ `d2b723fa…` (unchanged HEAD; not committed).

### Next Steps

- Codex independent re-review of findings 1–7.
- Optional owner approval later if H.264/OpenH264 MP4 is preferred over WebM for demos.

---

## 2026-08-25 02:30 — Correction Pass 2: run-scoped results + atomic delete audit

### Objective

Fix the two remaining Codex HIGH blockers: (1) zero-detection reprocessing resurrecting legacy NULL detections / hiding completed annotated replay behind failed later attempts; (2) permanent delete leaving orphaned quarantine when audit insertion failed after cascade commit.

### Summary

Introduced explicit `results_run_scoped` ownership so completed run-attributed runs are authoritative even at zero detections, separated latest-attempt vs current-completed-result in UI/status payloads, and made cascade deletion + durable system audit one SQLite transaction with quarantine restore / post-commit purge-warning semantics.

### Files Modified

#### Created

- `database/migrations/006_results_run_scoped.sql` — forward-only additive migration notes + backfill policy
- `tests/test_correction_pass2.py` — adversarial tests for zero-result authority, attempt/result separation, atomic delete/audit, FS restore, purge warning

#### Modified

- `database/sqlite_adapter.py` — migration 006, create_processing_run marks scoped=1, authoritative/current-result helpers, detection/analytics scope without COUNT(*) fallback, `delete_video_cascade_with_audit`, `list_system_audit_events`
- `database/schema.sql` — `results_run_scoped` on `processing_runs`
- `app.py` — `_db_video_to_ui` / `_build_status_payload` / history use latest attempt vs current completed result
- `core/video_lifecycle.py` — atomic cascade+audit; restore on DB failure; cleanup warning on post-commit purge failure
- `static/js/live_monitor.js` — history labels for current result vs latest attempt; show run detection_records
- `tests/test_upload_processing_ux.py` — mock `delete_video_cascade_with_audit` for DB-failure restore test
- `docs/development_logs/2026-08-25_to_2026-08-27.md` — this entry

### Detailed Changes

1. **Run attribution policy:** New runs insert `results_run_scoped=1`. Idempotent migration 006 adds the column and backfills UX/processor-era runs (`queued_at`/`stage`/`diagnostics_json`/`geometry_snapshot_json` or attributed children). Current detections/candidates use that authoritative completed run even when empty; legacy NULL rows only when no scoped completed run exists.
2. **Attempt vs result:** Latest processing run drives activity/error; `get_current_completed_result_run()` drives annotated replay + current-result marking. Failed/queued/running attempts no longer clear prior annotated_ready.
3. **Analytics:** Detection records, class counts, and violation candidates use the same scoped/legacy population (no zero totals with nonzero class mixes).
4. **Atomic delete:** `delete_video_cascade_with_audit` performs cascade + audit insert in one `db_session` transaction. Lifecycle restores quarantine on any DB failure; purge failure after commit returns success with `cleanup_warning` / `quarantine_token`.

### Testing Performed

- `python -m pytest tests/test_correction_pass2.py -q --tb=short` — 13 passed
- `python -m pytest tests/test_upload_processing_ux.py -q --tb=short` — 34 passed
- `python -m pytest tests/ -q --tb=line` — 257 passed in 71.49s
- `git diff --check` — exit 0 (CRLF warnings only)
- Adversarial A–D script: all PASS
- Live DB video 6 history API: detection_records=0, class_counts={}, processing_run_id=1, result_scope=run (was 9329/car:8640 before)
- Analytics API today: detection_records=0, class_counts={}, runs=1

### Project Progress

- Correction pass 2 ready for narrow Codex re-review of these two findings only on dirty branch `cursor/phase-1-foundation` @ `d2b723fa…` (HEAD unchanged; not committed).

### Next Steps

- Narrow independent Codex re-review of blockers 1–2 only.
- Do not commit until Codex verifies.

---

## 2026-08-25 02:55 — Migration 007: legacy result-scope provenance repair

### Objective

Fix migration 006 false-positive scoping that hid Video 6’s 9,329 NULL-attributed detections when Run 1 had diagnostics/geometry but no attributed child rows. Add explicit provenance so new zero-result runs remain authoritative without misclassifying legacy runs.

### Summary

Corrected migration 006 to backfill `results_run_scoped` only from attributed child rows. Added forward migration 007 (`results_scope_explicit`, `results_scope_origin`) that repairs already-misclassified databases, promotes attributed historical runs, and preserves explicit create-time zero-result ownership on re-application. `create_processing_run()` now records explicit provenance (`create`). Live Video 6 returned to legacy scope with all 9,329 detections visible; no result rows deleted.

### Files Modified

#### Created

- `database/migrations/007_results_scope_provenance.sql` — documentation for provenance + repair policy
- `tests/test_migration_007_provenance.py` — false-positive, repair, attributed, zero-result, idempotence, analytics, Video-6-equivalent, sparse-schema tests

#### Modified

- `database/sqlite_adapter.py` — `_attributed_child_exists_clauses`, corrected `_apply_migration_006`, new `_apply_migration_007`, `create_processing_run` provenance insert, wire 007 into `_run_migrations`
- `database/schema.sql` — `results_scope_explicit`, `results_scope_origin` on `processing_runs`
- `database/migrations/006_results_run_scoped.sql` — policy docs: metadata is not provenance
- `tests/test_correction_pass2.py` — assert create provenance; migration 006 metadata-only stays unscoped
- `docs/development_logs/2026-08-25_to_2026-08-27.md` — this entry

### Detailed Changes

1. **Ownership policy:** Legacy/ambiguous runs stay unscoped unless they have attributed children or explicit create provenance. Diagnostics, geometry, stage, queued_at, progress, and annotated artifacts do not prove ownership.
2. **Migration 006:** Adds `results_run_scoped` if missing; backfills only via attributed detections/review/violations.
3. **Migration 007:** Adds provenance columns; marks attributed historical runs `explicit` with origin `attributed_children`; resets `results_run_scoped=0` when scoped without explicit provenance and without attributed children.
4. **Create path:** New runs insert `results_run_scoped=1`, `results_scope_explicit=1`, `results_scope_origin='create'`.
5. **Live DB:** Video 6 Run 1 repaired to unscoped/non-explicit; 9,329 NULL detections remain current under `result_scope=legacy`.

### Testing Performed

- `python -m pytest tests/test_correction_pass2.py -q --tb=short` — 13 passed
- `python -m pytest tests/test_migration_007_provenance.py -q --tb=short` — 8 passed
- `python -m pytest tests/test_upload_processing_ux.py -q --tb=short` — 34 passed
- `python -m pytest tests/ -q --tb=line` — 265 passed in 73.93s
- `git diff --check` — exit 0 (CRLF warnings only)
- Video 6 after repair: detection_records=9329, class_counts={car:8640,truck:689}, result_scope=legacy, processing_run_id=NULL, Run1 scoped=0 explicit=0
- Isolated temp DB: legacy visible → explicit zero-result run → summary 0 → reapply migrations keeps scoped

### Project Progress

- Final known UX review blocker (migration 006 legacy misclassification) corrected on dirty branch `cursor/phase-1-foundation` @ `d2b723fa…` (HEAD unchanged; not committed).

### Next Steps

- Request final narrow Codex verification of migration 007 / provenance only.
- Do not commit until Codex verifies.

---

## 2026-08-25 03:30 — Selective release commit and push (processing UX)

### Objective

Owner-authorized selective staging, one commit, and push of the independently approved uploaded-video processing UX and integration corrections to `origin/cursor/phase-1-foundation` for demo-laptop transfer. Exclude weights, runtime media, databases, caches, temps, and local IDE settings.

### Summary

Hardened `.gitignore` for weights/engines, SQLite WAL/SHM, caches, temp review dirs, annotated/frames runtime outputs, and quarantine partials. Selectively staged 75 approved source/migration/test/docs/config paths (explicit `git add -- <path>` only). Left `HERMES_CONFIG.md`, `idea.md`, and `.cursor/settings.json` unstaged (machine-local paths / IDE). Verified focused and full suites green, then committed and pushed the branch without amend/reset/force.

### Files Modified

#### Modified

- `.gitignore` — added `*.pth`/`*.onnx`/`*.engine`, `runs/`, frames/annotated runtime ignores with `.gitkeep` exceptions, SQLite `-wal`/`-shm`, `.pytest_cache/`, `.tmp_docx_review/`, `.cursor/settings.json`
- `docs/development_logs/2026-08-25_to_2026-08-27.md` — this release entry

#### Staged for commit (approved implementation; not re-listed in full)

Approved app/core/database/migrations 003–007, templates, static JS/CSS, tests, Phase 2 training/registry docs and configs, and prior period development log — see commit `git show --stat`.

#### Explicitly excluded (left on disk)

- `.cursor/settings.json`, `HERMES_CONFIG.md`, `idea.md`
- `database/tavidm.db`, `dataset/annotated/run_*`, `.tmp_docx_review/`, `.pytest_cache/`, `__pycache__/`, `*.pt` (including `yolov8m.pt` / `yolov8s.pt`)

### Testing

- `python -m pytest tests/test_correction_pass2.py -q --tb=short` — 13 passed
- `python -m pytest tests/test_migration_007_provenance.py -q --tb=short` — 8 passed
- `python -m pytest tests/test_upload_processing_ux.py -q --tb=short` — 34 passed
- `python -m pytest tests/ -q --tb=line` — 265 passed
- `git diff --check` / `git diff --cached --check` — exit 0 (CRLF warnings only)
- No weights/videos/DBs/caches/secrets in staged name list; no binary staged files

### Next Recommended Steps

- On demo laptop: pull `cursor/phase-1-foundation`, copy `best.pt` via USB into local weights path, configure model path, smoke-test with known-good video.

### Completion Status

✅ Completed (commit + push verification follows in same session)

---
