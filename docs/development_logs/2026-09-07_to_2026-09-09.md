# Development Log

**Project:** TAVIDM

**Development Period:** 2026-09-07 to 2026-09-09

**Repository:** TAVIDM

**Primary Branch:** cursor/phase-1-foundation

**Last Updated:** 2026-09-07 17:54

---

# Overview

This document records all completed development work performed during this three-day development period.

Every log entry represents completed work only.

The documentation should always match the current repository state.

Do not document speculative work, future plans, or partially reverted implementations.

---

# Development Entries

---

## 2026-09-07 01:15 — Legal-Policy Foundation Repair (Stage B)

### Objective

Repair the partial legal-policy foundation so additive persistence definitions agree, policy approval requires explicit supervisory permission, printing attestation cannot bypass case confirmation, and proposed legal wording cannot be mistaken for verified classification. FastALPR, recurrence matching, engine fusion, and UI were out of scope.

---

### Summary

Aligned migration 008, schema.sql, and inline DDL; added migration 009 to upgrade conflicting 008 variants while preserving rows. Enforced `approve_policy` grants for activation (admin role alone cannot approve). Unified notice-print attestation behind a shared prerequisite check. Separated proposed vs verified classification APIs in `core/violation_policy.py`. Focused legal/persistence/roster/review tests passed (116).

---

### Files Modified

#### Created

- `database/migrations/009_legal_policy_schema_repair.sql`
  - Canonical notes for the additive repair migration.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - Period log for this development window.

#### Modified

- `database/migrations/008_legal_policy_persistence.sql`
  - Canonical DDL: `source_document_id`, `UNIQUE(violation_id, canonical_rule)`, nullable policy-audit `violation_id`.
- `database/schema.sql`
  - Matched fresh-install DDL to the repaired 008 shape.
- `database/sqlite_adapter.py`
  - Inline 008 sync, migration 009 repair, approval/print gates, plate/recurrence write fixes.
- `database/db.py`
  - Existing Stage B facade exports retained (no new public surface required).
- `core/violation_policy.py`
  - Proposed vs verified classification APIs; stricter recurrence eligibility.
- `tests/test_legal_policy_persistence.py`
  - Upgrade fixtures, approval/print regressions, multi-behavior persistence.
- `tests/test_violation_policy.py`
  - Proposed/verified separation and partially_verified gating.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Marked completed Stage A/B foundation items only; full-suite regression remains open.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Confirmed and repaired DDL drift among schema.sql, migration 008, and inline fallback (`source_document_id`, multi-behavior uniqueness, nullable `case_action_events.violation_id` for policy-level audit).
- Added `_apply_migration_009` to upgrade older 008 table variants without dropping preserved rows; tested against temporary conflicting fixtures only.
- Removed admin-as-supervisor role alias. `can_approve_policy` requires explicit `approve_policy` permission; `approve_legal_policy_version` enforces the check before writes.
- Restricted case confirm / print attestation role grants to enforcer; admin/viewer cannot attest by role alone.
- Shared `_attest_notice_print_action` for both print paths; requires authenticated `case_confirmed` audit event; retries are idempotent.
- `official_category_for` now returns only verified classifications; `proposed_official_category_for` exposes research wording; penalties gated by verified status.
- Fixed `record_plate_verification` / `record_recurrence_review` to write only supported columns/bindings.

---

### Technical Explanation

Row identity for `case_policy_records` is `UNIQUE(violation_id, canonical_rule)` so fused parking/obstruction contributing behaviors can both be retained without inventing a second case row type. Policy audit events use NULL `violation_id` because they are not case-scoped and FK enforcement rejects sentinel `0`. Migration 009 rebuilds only when SQLite table SQL shows the older constraint/nullability shapes, keeping reapplication safe on already-corrected databases.

---

### Testing Performed

#### Test Commands

```bash
python -m pytest tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py -v --tb=line
git diff --check -- database/schema.sql database/sqlite_adapter.py database/db.py database/migrations/008_legal_policy_persistence.sql database/migrations/009_legal_policy_schema_repair.sql core/violation_policy.py tests/test_legal_policy_persistence.py tests/test_violation_policy.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- 116 passed, 0 failed, 0 skipped
- Migration fixtures: UNIQUE(violation_id)-only + missing `source_document_id` + NOT NULL policy audit `violation_id` upgraded with row preservation verified
- `git diff --check` clean after EOF newline fix
- Interpreter: `C:\Program Files\Python311\python.exe`
- Test DBs used temporary `test_tavidm.db` paths via `SQLITE_PATH` / `DATABASE_URL`; operational `database/tavidm.db` was not migrated

#### Coverage Notes

- Focused foundation repair coverage only; full suite and Flask/UI wiring not executed

---

### Issues Encountered

- Conflicting 008 drafts left CREATE TABLE IF NOT EXISTS unable to heal uniqueness/nullability; resolved with additive migration 009 rebuilds.
- `insert_violation` FK requires a real video row; test fixtures updated accordingly.
- `record_plate_verification` attempted unsupported `processing_run_id` column; folded into diagnostics JSON.

---

### Project Progress

#### Completed

- Stage A legal-policy module + tests (focused)
- Stage B persistence contract repair, approval/print gates, focused regressions

#### In Progress

- Stage B full-suite regression still open

#### Remaining

- Stage C FastALPR / event-time wiring
- Stage D recurrence matcher and print batching
- Stage E Flask/UI integration
- Engine fusion integration for parking/obstruction cases

---

### Next Steps

1. Optional: run broader non-inference pytest suite for Stage B regression checkbox.
2. Next implementation stage after approval: FastALPR adapter + event-time provenance (Stage C), or engine fusion wiring if prioritized.

---

### Notes

- No real accounts granted permissions; no operational DB migration; no training/model changes; no git commit/push.
- Legal mappings remain unverified/flag_only; no legal research performed.
- Authorship of prior uncommitted related files remains unknown.

---

## 2026-09-07 01:16 — Legal-Policy Inactive User and Dismissed-Case Gates

### Objective

Fix two independently reproduced legal-policy foundation defects: inactive users retaining policy/enforcement authority, and dismissed cases acquiring case-confirmation / notice-printing events. Implement repairs and focused regression tests only; do not begin Stage C.

---

### Summary

Authorization helpers now require an existing active user before role or explicit-capability evaluation, and write paths enforce those helpers (including policy proposal). Case confirmation and both notice-print attestation entry points reject current `dismissed` status inside the write transaction before any idempotent success return, preserving historical audit events without reopening cases. Focused legal/persistence/roster/review suite: 124 passed.

---

### Files Modified

#### Created

- None.

#### Modified

- `database/sqlite_adapter.py`
  - Active-account gate on `can_*` helpers; propose write enforces `can_propose_policy`; dismissed-status gate in `confirm_case` and `_attest_notice_print_action`.
- `tests/test_legal_policy_persistence.py`
  - Regression coverage for inactive authority and dismissed confirm/print blocking.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Recorded the two Stage B authorization/state repairs; full-suite regression remains open.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Added `_user_account_is_active` and applied it in `can_confirm_case`, `can_attest_print`, `can_approve_policy`, and `can_propose_policy`.
- `propose_legal_policy_version` now raises `PermissionError` when the proposer lacks active propose authority (previously only checked user existence).
- `confirm_case` and `_attest_notice_print_action` read current `violations.status` and reject `dismissed` before idempotent “already done” returns.
- Both public print functions continue to share the helper, so checks are equivalent.
- Historical `case_action_events` and stored permission grants are left intact when accounts are inactive or cases are dismissed.

---

### Technical Explanation

Inactivity is evaluated before role/capability checks so explicit grants cannot restore authority for deactivated accounts. Dismissed-case blocking is a current-state check inside the write transaction, ordered ahead of idempotent early returns, so previously confirmed or printed cases that were later dismissed cannot silently succeed again or gain new audit rows. No schema migration was required.

---

### Code Impact

- Policy proposal now consistently requires propose authority at the adapter write boundary.
- Confirm/print operations remain permission-gated and additionally status-gated for dismissal.
- No Flask/UI, FastALPR, recurrence, or engine-fusion changes.

---

### Database Changes

None (authorization/state-check repairs only; no migration).

---

### API Changes

None (behavior tightened for existing adapter functions; no new public endpoints).

---

### UI / UX Changes

None.

---

### Testing Performed

#### Test Commands

```bash
"C:\Program Files\Python311\python.exe" -m pytest tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py -v --tb=short
git diff --check -- database/sqlite_adapter.py tests/test_legal_policy_persistence.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- 124 passed, 0 failed, 0 skipped (exit 0)
- `git diff --check` exit 0
- Temporary `SQLITE_PATH` / `DATABASE_URL` fixtures only; operational DB and real accounts untouched

#### Coverage Notes

- New classes: `TestInactiveUserAuthority`, `TestDismissedCaseBlockedActions`
- Exercises both `confirm_notice_printed` and `confirm_notice_printer_attested`

---

### Issues Encountered

None beyond the two reproduced defects, which were confirmed still present before repair.

---

### Project Progress

#### Completed

- Inactive-user authority rejection for confirm/print/propose/approve
- Dismissed-case block for confirm and both print attestations
- Focused regression coverage for both defects

#### In Progress

- Stage B full-suite regression still open
- Stage C not started (awaiting owner authorization after independent review)

#### Remaining

- Stage C FastALPR / event-time
- Stage D recurrence matcher and print batching
- Stage E Flask/UI integration

---

### Next Steps

1. Independent review of these two repairs.
2. Owner authorization before Stage C.

---

### Notes

- No operational database access, real-account changes, training/model/package changes, commits, pushes, or Stage C work.
- Do not mark entire Stage B foundation complete solely because these two repairs pass.

---

## 2026-09-07 01:44 — Stage C1 Standalone FastALPR Adapter (Mocked)

### Objective

Implement an import-safe, testable FastALPR plate-processing adapter with mocked regression tests only. No package install, model download, real inference, or application integration.

---

### Summary

Added `core/plate_processing.py` with injected-backend `PlateProcessor`/`read_plate`, machine-only `PlateProcessingOutcome` results, and deferred real-backend factory (upstream default `ALPR(...)` can hub-download). Mocked tests in `tests/test_plate_processing.py` cover import safety, candidate semantics, failure contracts, and isolation. Focused suite including new tests: 140 passed.

---

### Files Modified

#### Created

- `core/plate_processing.py`
  - Standalone FastALPR-compatible adapter; no import-time init or DB writes.
- `tests/test_plate_processing.py`
  - Mocked upstream-shaped backend fixtures and contract tests.

#### Modified

- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Stage C1 contract, completed mocked adapter items; Stage C not marked complete.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Machine outcomes separated from human `plate_verifications.plate_status` values; adapter never emits `verified_readable` or grants recurrence eligibility.
- Empty backend list maps to `no_candidate_detected` (explicitly not `not_visible`).
- Confidences preserved or left unavailable; no coercion to zero; no accepted-plate identity field.
- Defensive image copy before backend predict; reusable injected backend.
- `real_fast_alpr_backend_factory_status()` documents deferred live construction.

---

### Technical Explanation

Upstream `ALPR.predict` returns `list[ALPRResult]` with `detection` (`BoundingBox` x1/y1/x2/y2) and optional `OcrResult` (`text`, `confidence` float|list). The adapter normalizes that shape via an injected protocol so tests need no package. Default hub construction is not wrapped because it can download models without local paths.

---

### Code Impact

- No pipeline, Flask, DB, or YOLOv8m detector changes.
- Future callers construct `PlateProcessor(backend)` after a violation/evidence event and call `read_plate(crop)`.

---

### Database Changes

None.

---

### API Changes

None (new core module only; no Flask routes).

---

### UI / UX Changes

None.

---

### Testing Performed

#### Test Commands

```bash
"C:\Program Files\Python311\python.exe" -m pytest tests/test_plate_processing.py -v --tb=short
"C:\Program Files\Python311\python.exe" -m pytest tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py tests/test_plate_processing.py -v --tb=short
git diff --check -- core/plate_processing.py tests/test_plate_processing.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- Plate adapter alone: 16 passed (exit 0)
- Focused suite + adapter: 140 passed (exit 0)
- `git diff --check` exit 0

#### Coverage Notes

- Mocked backends only; no FastALPR runtime or Philippine CCTV accuracy claim

---

### Issues Encountered

- Import-guard test initially treated `fast_alpr` as a hard failure instead of `ImportError`; corrected so availability probe remains fail-closed.

---

### Project Progress

#### Completed

- Stage C1 standalone mocked FastALPR adapter + tests

#### In Progress

- Stage C remainder (install/models, integration, human verify, event-time) deferred
- Stage B full-suite regression still open

#### Remaining

- FastALPR install + local model config (approval gate)
- Pipeline integration after violation evidence
- Event-time helpers (separate task)
- Stage D/E work

---

### Next Steps

1. Independent review of Stage C1.
2. Next smallest authorized task: FastALPR dependency/model configuration approval gate, or Stage C2 event-time helpers — do not start without owner authorization.

---

### Notes

- No dependency install, model download, operational DB, training, commits, or pushes.
- Prior Stage B authorization repairs were not reopened.

---

## 2026-09-07 02:12 — FastALPR Adapter Malformed-Result Hardening

### Objective

Repair three reproduced malformed-result defects in the standalone FastALPR adapter: uncaught infinite bounding-box `OverflowError`, dictionary OCR text stringification, and confidence-list entry dropping. Focused hardening and regression tests only.

---

### Summary

Contained non-finite/invalid box parsing under an explicit `retain_candidate_without_box` policy; rejected non-string OCR text without fabrication; made confidence lists fail closed when any entry is bad while preserving valid list length/order. Added mocked regressions. Focused suite: 150 passed.

---

### Files Modified

#### Created

- None.

#### Modified

- `core/plate_processing.py`
  - Hardened `_parse_bounding_box`, `_preserve_ocr_text`, `_preserve_confidence`, and item parse notes/diagnostics.
- `tests/test_plate_processing.py`
  - `TestMalformedBoundingBox`, `TestMalformedOcrText`, `TestConfidencePreservation`.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Documented hardening contract; Stage C not marked complete.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Box coords require finite numeric conversion; `OverflowError`/`NaN`/`inf` no longer escape processing.
- Invalid box drops the box only; candidate kept when label/confidence/OCR remain.
- OCR text accepts only `str` (including empty); dict/list/number/bool/bytes/object → `ocr_raw=None` with diagnostic count.
- Confidence: any bad list entry → entire field `None`; non-finite scalars unavailable; detection/OCR/region use consistent normalization.
- Diagnostics use counters/flags only (no OCR text or exception messages).

---

### Technical Explanation

Smallest compatible confidence fix is fail-closed list normalization so positional OCR character confidence is never silently shortened. Box policy prefers retaining detection-only/OCR context over discarding the whole candidate when coordinates alone are bad.

---

### Code Impact

Adapter-only; no pipeline, DB, FastALPR install, or Stage C2 work.

---

### Database Changes

None.

---

### API Changes

None (result field semantics tightened for malformed inputs only).

---

### UI / UX Changes

None.

---

### Testing Performed

#### Test Commands

```bash
"C:\Program Files\Python311\python.exe" -m pytest tests/test_plate_processing.py -v --tb=short
"C:\Program Files\Python311\python.exe" -m pytest tests/test_plate_processing.py tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py -v --tb=short
git diff --check -- core/plate_processing.py tests/test_plate_processing.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- Plate adapter: 26 passed (exit 0)
- Focused suite: 150 passed (exit 0) [was 140]
- Temporary DB fixtures confirmed (`SQLITE_PATH`/`DATABASE_URL` + TemporaryDirectory)
- `git diff --check` exit 0

#### Coverage Notes

- Mocked only; no install/inference/integration

---

### Issues Encountered

None beyond the three reproduced defects, which were confirmed still present before repair.

---

### Project Progress

#### Completed

- Stage C1 malformed-result hardening for box/OCR/confidence

#### In Progress

- Stage C install/integration/event-time still deferred

#### Remaining

- FastALPR install + local model config (approval)
- Pipeline integration; Stage C2 event-time; Stage D/E

---

### Next Steps

1. Independent review of these three repairs.
2. Do not start Stage C2 / install / integration without owner authorization.

---

### Notes

- No dependencies, models, operational data, training, commits, or pushes touched.

---

## 2026-09-07 02:35 — Stage C2: Standalone Event-Time Provenance Helpers

### Objective

Implement isolated event-time provenance helpers for TAVIDM without integrating into the application, database, video pipeline, or recurrence matcher. Preserve Stage C1 work; deliver a clear contract for candidate time, source, reliability, confirmation, timezone handling, and corrections.

---

### Summary

Added `core/event_time.py` with immutable structured results and pure helpers for CCTV, video-metadata, and user-entry claims; confirmation/correction returns new snapshots that retain original provenance. Narrow ISO-8601 parsing preserves explicit offsets, refuses host-local invention, and returns unresolved results when named zones are unavailable. Focused tests cover the approved reliability/conflict/confirmation/timezone behaviors; Stage C is not marked complete.

---

### Files Modified

#### Created

- `core/event_time.py`
  - Standalone event-time provenance helpers and result contract.
- `tests/test_event_time.py`
  - Focused deterministic coverage for the Stage C2 success criteria.

#### Modified

- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Documented Stage C2 contract, checklist items, and integration prerequisites; Stage C remains incomplete.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Structured types: `EventTimeInstant`, `EventTimeClaim`, `EventTimeResult`, `ReviewConfirmation`, `ParseResult`.
- Enums: `EventTimeSource` (event-capable + non-event clocks), `CctvReliability`, `EventTimeUsability`, `ParseStatus`.
- Builders: `from_cctv_timestamp`, `from_video_metadata`, `from_user_entry`, `missing_event_time`, `reject_non_event_source`, `resolve_event_time`.
- `confirm_event_time` accepts/corrects without mutating prior; incomplete metadata fails closed.
- `parse_iso8601_event_instant` + `usable_event_instant` (time readiness only).
- Named-zone localization via stdlib `zoneinfo` with ambiguous/nonexistent detection; no new timezone packages.

---

### Technical Explanation

Reuses persistence source labels from `record_event_time` but remains import-isolated from the DB adapter. Distinct from `recording_time.normalize_recorded_at` (naive upload-path video start strings). Reliable CCTV may be usable; metadata and unreliable CCTV stay confirmation-gated; processing/upload/file-modification/printing never become automatic event time. Conflicts return `CONFLICT` without auto-selection. Reviewer identity is a preserved claim, not authentication.

---

### Code Impact

Helpers only. No Flask, pipeline, DB, recurrence, FastALPR, or legal-policy wiring. Callers may later persist via `record_event_time` without erasing CCTV provenance on confirmation.

---

### Database Changes

None.

---

### API Changes

None (no HTTP/routes). New pure Python APIs in `core/event_time.py` only.

---

### UI / UX Changes

None.

---

### Testing Performed

#### Test Commands

```bash
"C:\Program Files\Python311\python.exe" -m pytest tests/test_event_time.py -v --tb=short
"C:\Program Files\Python311\python.exe" -m pytest tests/test_event_time.py tests/test_plate_processing.py tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py -v --tb=short
git diff --check -- core/event_time.py tests/test_event_time.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- Event-time only: 32 passed, 1 skipped (exit 0) — DST ambiguity skip (no tzdata on host)
- Focused suite: 182 passed, 1 skipped (exit 0) [was 150]
- Temporary DB fixtures confirmed for legal/review suites (`SQLITE_PATH`/`DATABASE_URL` + TemporaryDirectory)
- `git diff --check` exit 0

#### Coverage Notes

- Deterministic offsets; no machine clock/host TZ for event values
- Named-zone success path asserted when ZoneInfo available; otherwise UNRESOLVED contract asserted

---

### Issues Encountered

Host has no IANA tzdata (`ZoneInfo` fails for Asia/Manila/UTC/Australia/Sydney). Module correctly returns `UNRESOLVED` rather than guessing; ambiguity DST test skipped on this environment.

---

### Project Progress

#### Completed

- Stage C2 standalone event-time helpers + focused tests + checklist contract

#### In Progress

- Stage C remainder (FastALPR install/models, plate/event-time persistence & UI, pipeline wiring)

#### Remaining

- Persist event-time history; authenticated confirmation; CCTV OCR/reliability; Stage D recurrence matcher; Stage E integration

---

### Next Steps

1. Independent review of Stage C2 helpers/tests.
2. Do not begin DB/UI/pipeline/recurrence integration without separate authorization.

---

### Notes

- No operational data, training, dependency installs, commits, or pushes.
- Stage C not marked fully complete.

---

## 2026-09-07 02:45 — Stage C2 Event-Time Defect Repairs (3)

### Objective

Repair three independently reproduced defects in standalone event-time helpers: invalid UTC offset handling, reconfirmation restoring a pre-correction candidate, and order-dependent multi-source resolution. No application integration.

---

### Summary

Validated explicit offsets before timezone construction (`INVALID` for bad minutes/magnitudes, no crash/normalization). Reconfirmation now retains an existing confirmed event time unless a new correction is supplied. Multi-source agreement is order-independent and returns `REQUIRES_CONFIRMATION` with all claims in `contributing_claims` (no source-priority invention). Focused suite: 204 passed, 1 skipped.

---

### Files Modified

#### Created

- None.

#### Modified

- `core/event_time.py`
  - `_parse_offset`, `parse_iso8601_event_instant`, `confirm_event_time`, `resolve_event_time` / `_resolve_multi_source`; added `contributing_claims`.
- `tests/test_event_time.py`
  - Regression coverage for offsets, reconfirmation, and multi-source order independence.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Documented offset range, reconfirmation rules, multi-source/`contributing_claims` contract.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

1. **Offsets:** Reject `MM>59` and `HH>23` / `±24:00` as `ParseStatus.INVALID`; never normalize `+08:60` → `+09:00`; never raise uncaught `ValueError`.
2. **Reconfirmation:** Prefer `prior.confirmed_event_time`, else `prior.candidate`, else require correction; diagnostics distinguish `accepted_confirmed_value` vs `accepted_candidate`.
3. **Multi-source:** Agreeing claims and mixed resolved/unresolved → `REQUIRES_CONFIRMATION` with agreed candidate when known; conflicts remain `CONFLICT`; single-source reliable CCTV unchanged.

---

### Technical Explanation

Source-priority among agreeing CCTV/metadata claims remains an unresolved product decision; helpers preserve confirmation-required behavior rather than inventing priority. `contributing_claims` is a small backward-compatible contract extension (default empty tuple).

---

### Code Impact

Helpers/tests/docs only. No DB, UI, pipeline, recurrence, OCR, or dependency changes.

---

### Database Changes

None.

---

### API Changes

None (HTTP). Pure helper contract extended with `contributing_claims` and clarified confirmation diagnostics.

---

### UI / UX Changes

None.

---

### Testing Performed

#### Test Commands

```bash
"C:\Program Files\Python311\python.exe" -m pytest tests/test_event_time.py -v --tb=short -rs
"C:\Program Files\Python311\python.exe" -m pytest tests/test_event_time.py tests/test_plate_processing.py tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py -v --tb=short -rs
git diff --check -- core/event_time.py tests/test_event_time.py docs/IMPLEMENTATION_CHECKLIST.md
```

#### Results

- Event-time: 54 passed, 1 skipped (exit 0)
- Focused suite: 204 passed, 1 skipped (exit 0) [was 182]
- Temp DB isolation confirmed
- `git diff --check` exit 0

#### Coverage Notes

- Named-timezone DST ambiguity still skipped (no tzdata); not required for these three repairs

---

### Issues Encountered

None beyond the three reproduced defects, which are fixed.

---

### Project Progress

#### Completed

- Stage C2 event-time defect repairs (offsets, reconfirmation, multi-source order)

#### In Progress

- Stage C remainder (install/models, persistence, UI, pipeline)

#### Remaining

- Source-priority policy decision (if auto-usable multi-source agreement is desired later)
- Persist/auth/OCR/recurrence/integration (separate authorization)

---

### Next Steps

1. Independent review of these three repairs.
2. Do not begin integration or Stage D without owner authorization.

---

### Notes

- No operational data, training, packages, commits, or pushes.
- Stage C not marked complete.

---

## 2026-09-07 03:15 — Case Identity, Review Persistence, Recurrence, UI Integration

### Objective

Complete the remaining safely authorized violation-policy implementation: stable case identity with parking/obstruction grouping, plate and event-time review persistence, recurrence matching with unresolved-boundary handling, Flask/Bootstrap review and printing actions, legal-register identity notes without activating mappings, reports/E2E tests, and an updated checklist/handoff. No operational migration, FastALPR install, commits, or production activation.

---

### Summary

Integrated case grouping and review materialization so overlapping Illegal Parking + Obstruction observations become one logical case with retained contributing behaviors. Wired authorized plate/event-time persistence, recurrence evaluation (history visible; offense suggestions disabled until CTEU-approved policy), notice-print batching, and Flask/UI surfaces. Confirmed CO248/CO944 titles on the city regulatory register without promoting any mapping to verified. Focused suite: 240 passed, 1 skipped.

---

### Files Modified

#### Created

- `core/case_identity.py`
  - Observation grouping, episode overlap, fused-case assembly helpers.
- `core/case_review_service.py`
  - Application service for materialize/verify plate/persist event-time.
- `core/recurrence_policy.py`
  - Match, eligibility, summarize, persist (suggestion-only).
- `tests/test_case_identity.py`
  - Grouping, fusion, run boundaries, evidence, idempotency.
- `tests/test_case_review_service.py`
  - Plate/event-time auth, corrections, unavailable OCR honesty.
- `tests/test_recurrence_policy.py`
  - Matching exclusions, inactive lookback, policy approval, print batch.
- `tests/test_case_policy_e2e.py`
  - Isolated end-to-end + Flask API coverage with fake plate path.

#### Modified

- `database/sqlite_adapter.py`
  - Case-identity helpers, plate/event-time permissions, print batch, config policy snapshot version, recurrence candidate query.
- `app.py`
  - Confirm uses materialize; case/policy APIs; enriched UI mappers.
- `core/reports.py`
  - Official category / case-outcome reporting without truncating Excel wording.
- `static/js/violations.js`
  - Case review actions in detail modal; plate status badges.
- `static/js/review_queue.js`
  - Legal status / proposed category / OCR-unavailable notes.
- `config/violation_legal_mappings.json`
  - Register identity notes for CO248/CO944; mappings remain unverified/flag_only.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Ordered queue, stage evidence, deferred gates.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

#### Renamed

- None.

#### Deleted

- None.

---

### Detailed Changes

- Parking/obstruction fusion groups by video + processing_run + track + overlapping episode (or proximity fallback); creates one violation; stores both canonical rules in `case_policy_records`; links all contributing reviews; preserves evidence.
- Plate verification requires active enforcer (or grant); `verified_readable` needs full accepted text; unclear/not_visible clear identity; legacy `violations.plate_status` mapped to recognized/unreadable CHECK values.
- Event-time persistence uses helpers; timestamp OCR flagged unavailable; corrections preserve previous confirmed values from audit history.
- Recurrence defaults to 365-day lookback search but does not enable first/second/third labels without approved `offense_suggestions_enabled`.
- Unresolved edges block offense suggestions while still allowing history display.
- Printable preview is explicitly not Notice Printed; attestation is a separate authorized action; batch returns per-case results.
- Reports distinguish observation/grouped-case/outcome counts; Excel retains full official wording.

---

### Technical Explanation

Service-layer integration keeps standalone Stage C helpers pure while Flask routes derive actors from session. `ensure_config_mapping_policy_version` satisfies FKs for JSON-backed snapshots without activating lookback policy. Admin role still cannot approve policy or perform enforcement writes without explicit grants / enforcer role checks inside adapter functions.

---

### Testing Performed

- Interpreter: `C:\Program Files\Python311\python.exe`
- `SQLITE_PATH` / `DATABASE_URL` set to temporary test DBs only
- Command:

```text
python -m pytest tests/test_event_time.py tests/test_plate_processing.py tests/test_violation_policy.py tests/test_legal_policy_persistence.py tests/test_vehicle_class_schema.py tests/test_review_queue.py tests/test_case_identity.py tests/test_case_review_service.py tests/test_recurrence_policy.py tests/test_case_policy_e2e.py -q -rs
```

- Result: **240 passed, 1 skipped** (Australia/Sydney tzdata)
- `git diff --check`: no whitespace errors (CRLF warnings only)
- Browser validation: deferred
- Real FastALPR / GPU / training: not run
- Operational DB: not migrated

---

### Project Progress

#### Completed

- Stages A–B foundation (prior)
- Stage C library + authorized persistence/service wiring (mocked ALPR)
- Stage D recurrence module + print batch (suggestions inactive)
- Stage E Flask/UI/report/E2E integration for review workflow
- Legal register identity notes without activation

#### In Progress

- None for this authorized contract (remaining work is approval-gated)

#### Remaining

- Real FastALPR install/config and CCTV evaluation
- Owner/CTEU legal text verification and mapping promotions
- Supervisor grants + offense-suggestion policy activation
- Operational migration approval and deployment
- Isolated browser validation

---

### Next Steps

1. Independent review of integrated implementation.
2. Owner: obtain CO248/CO944 full text and decide mapping promotions.
3. Owner: authorize FastALPR local model configuration separately.
4. Owner: authorize operational migration only after backup/review.
5. Do not enable offense suggestions until CTEU-approved policy detail flag is set.

---

### Notes

- No commits, pushes, PRs, Mnemosyne writes, package installs, or live citations.
- Preexisting unrelated dirty-worktree files were left untouched except files listed above.
- Auto-migration on `app` import remains a deployment caution for the operational database.

---

## 2026-09-07 09:47 — Five Integration Defect Repairs (Identity/Auth/Plate/Recurrence/Event-Time)

### Objective

Repair five independently reproduced integration defects without operational migration, legal activation, training, or deployment: (1) case identity across episodes and late observations, (2) authorization inside review materialization, (3) atomic plate verification, (4) recurrence policy context preservation, (5) correction-only event-time persistence.

---

### Summary

Episode-aware case identity now keeps distinct parking/obstruction episodes separate while attaching late overlapping fusion members to the established case. `materialize_case_from_review` enforces `can_confirm_case` before any write or idempotent success. Plate verification/audit/legacy sync runs in one SQLite transaction. Case-detail recurrence evaluation loads the active policy’s lookback and version and preserves historical case policy versions. Correction-only event-time requests load the latest persisted snapshot with original provenance before applying a correction.

---

### Files Modified

#### Modified

- `core/case_identity.py`
  - Removed 60s proximity / assumed-FPS auto-merge; episode-uncertain observations stay separate; late-fusion finder skips non-fusion types and dismissed cases.
- `core/case_review_service.py`
  - Service-level `can_confirm_case`; sibling load includes confirmed reviews; atomic plate via `apply_plate_verification`; correction-only event-time snapshot reload with original confirmer/time.
- `core/recurrence_policy.py`
  - `load_active_recurrence_policy_context`; historical match versions from case snapshots; dismissed current case blocked; evaluation records lookback/version used.
- `database/sqlite_adapter.py`
  - `list_reviews_for_event`, episode-aware `find_fused_case_violation`, `apply_plate_verification` atomic write, `get_event_time_snapshot`, confirm+link in one transaction, orphan recovery helper.
- `app.py`
  - Confirm route uses `login_required` + service auth; case GET passes active lookback/version into recurrence evaluation.
- `tests/test_case_identity.py`
  - Distinct episodes, late fusion both directions, ambiguous timing, dismissed reuse, admin deny, supervisor grant.
- `tests/test_case_review_service.py`
  - Correction-only / multi-actor event-time; plate atomicity fault injection; verified→unclear clear.
- `tests/test_recurrence_policy.py`
  - Non-default lookback, historical vs current version gate, dismissed current case.
- `tests/test_case_policy_e2e.py`
  - Exact HTTP 403 for admin materialize/confirm; no dual success/denial pass.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Focused suite evidence updated to 260 passed / 1 skipped.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

---

### Detailed Changes

1. **Case identity:** Source/run/track is necessary but not sufficient. Overlap requires established episode bounds/timestamps. Missing timing no longer merges via 60s proximity or frame/30 FPS invention. Late obstruction after parking (and reverse) loads already-linked siblings, upgrades to fused Obstruction representation, and retains contributing rules/evidence. Distinct 10–20s vs 100–110s episodes create separate cases. Dismissed cases are not reused.
2. **Authorization:** `materialize_case_from_review` checks `can_confirm_case` before writes and before idempotent reuse returns. Admin-only and inactive accounts are denied. Explicit `confirm_case` grant allows non-enforcer supervisors. Confirm route no longer relies on `role_required("enforcer")` admin bypass as authorization.
3. **Plate atomicity:** `apply_plate_verification` writes plate row + `plate_verified` audit + legacy `violations.plate_*` in one `db_session`. Fault after any stage rolls all back. Unclear/not-visible clears current accepted identity while prior audits remain. Successful corrections store previous values in audit detail.
4. **Recurrence context:** Active policy lookback/version/offense flag loaded once via `load_active_recurrence_policy_context`. Historical matches carry their own `case_policy_records.policy_version_id` (never substituted with current). Cross-version remains unresolved. Dismissed current cases get no offense suggestion.
5. **Event-time correction:** Correction-only requests load `get_event_time_snapshot` first, reconstruct original source/claims/confirmer/time, record correction actor/time separately, and preserve prior value on invalid correction. Missing prior + correction is treated as a new user entry (no fabricated prior confirmation).

---

### Technical Explanation

- Episode overlap is positive-evidence only; uncertainty is exposed via `episode_identity_uncertain` rather than silent merge.
- Atomic plate persistence avoids multi-connection helpers that previously committed plate rows before audit/legacy sync.
- Recurrence version gating compares historical vs current *case* snapshot versions, not the live active policy id assigned to every match.

---

### Testing Performed

```text
Interpreter: C:\Program Files\Python311\python.exe
SQLITE_PATH / DATABASE_URL → temporary suite DB only

python -m pytest tests/test_event_time.py tests/test_plate_processing.py
  tests/test_violation_policy.py tests/test_legal_policy_persistence.py
  tests/test_vehicle_class_schema.py tests/test_review_queue.py
  tests/test_case_identity.py tests/test_case_review_service.py
  tests/test_recurrence_policy.py tests/test_case_policy_e2e.py
  -q -rs
→ 260 passed, 1 skipped, exit 0

git diff --check (changed repair files) → exit 0
```

---

### Project Progress

#### Completed

- Five integration defect repairs with adversarial regression coverage
- Focused suite 260 passed / 1 skipped (was 240 / 1)

#### In Progress

- Independent review of this repair pass

#### Remaining

- Operational migration approval
- Legal mapping verification / promotions
- FastALPR install and CCTV OCR
- Offense-suggestion policy activation
- Isolated browser validation

---

### Next Steps

1. Independent review of the five repairs and new regression tests.
2. Do not begin activation, grants, or operational migration from this pass.

---

### Notes

- No commits, pushes, Mnemosyne writes, training, package installs, or operational DB opens.
- Unrelated dirty-worktree changes outside the listed files were preserved.

---

## 2026-09-07 10:08 — Three Follow-up Integration Repairs

### Objective

Close three incomplete parts of the prior integration repair: false-success materialization retries, live-mapping recurrence verification, and original event-time candidate overwrite across corrections.

---

### Summary

Materialization retries now finish missing links/evidence/per-behavior policy snapshots and refuse success until complete, preserving the originally selected policy version. Recurrence suggestions require a verified applicable case-level snapshot for the evaluated rule (live config cannot retroactively verify). Event-time persistence keeps `original_candidate` distinct from `previous_confirmed` and `effective_confirmed` across reloads/corrections.

---

### Files Modified

#### Modified

- `core/case_review_service.py`
  - Recoverable materialization completion + intent; original event-time candidate load/persist.
- `core/recurrence_policy.py`
  - Case-snapshot applicable mapping gate for current and historical eligibility.
- `tests/test_case_identity.py`
  - Snapshot/partial-fusion retry, policy-version preservation, unauthorized retry.
- `tests/test_case_review_service.py`
  - Original candidate multi-correction / missing-provenance tests.
- `tests/test_recurrence_policy.py`
  - Case-snapshot verification matrix; verified snapshot helpers.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Evidence updated to 274 passed / 1 skipped.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

---

### Testing Performed

```text
C:\Program Files\Python311\python.exe -m pytest
  tests/test_event_time.py … tests/test_case_policy_e2e.py -q -rs
→ 274 passed, 1 skipped, exit 0
git diff --check → exit 0
```

---

### Notes

- Materialization remains recoverable staged writes (not one mega-transaction) with explicit completion and nonduplication via INSERT OR IGNORE / idempotent links.
- No operational migration, legal promotions, commits, or Mnemosyne writes.

---

## 2026-09-07 10:26 — Materialization Intent Atomic Recovery Repair

### Objective

Close the remaining materialization-recovery defect where the originally selected policy could be lost when intent creation failed after case/review confirmation had already committed. Do not reopen accepted recurrence snapshot or event-time fixes.

---

### Summary

Initial case creation, review confirmation/linkage, and materialization intent now share one SQLite transaction via `create_case_with_materialization_intent`. Intent insertion failure rolls the entire initial boundary back. Recovery of incomplete historical cases uses only durable intent or consistent snapshot provenance; missing or conflicting context returns an unresolved `CaseReviewError` without inventing the active policy. Evidence/snapshot completion remains staged and recoverable.

---

### Files Modified

#### Modified

- `database/sqlite_adapter.py`
  - Added `create_case_with_materialization_intent` (single connection/transaction; `_fail_after='intent'` test seam).
- `core/case_review_service.py`
  - New cases use the atomic initial write; `_resolve_original_policy_version` replaces unsafe active-policy fallback.
- `tests/test_case_identity.py`
  - Regression coverage for intent rollback, policy-change freshness, v1 retention, unresolved/conflict recovery, fused paths, dismissed/unauthorized.
- `docs/IMPLEMENTATION_CHECKLIST.md`
  - Evidence updated to 283 passed / 1 skipped.
- `docs/development_logs/2026-09-07_to_2026-09-09.md`
  - This entry.

---

### Detailed Changes

- Atomic initial writes: violation insert + primary (and calling) review confirm/link + `materialization_intent` audit.
- Later sibling confirmations, evidence merge, and policy snapshots stay outside that boundary.
- `_resolve_original_policy_version` collects intent + snapshot versions; empty → unresolved; >1 distinct → conflict; exactly one → recover under that version.
- Fault injection at the real intent boundary asserts persisted rollback (no orphan violation/link/intent).

---

### Database Changes

None (additive table/schema changes not required; existing `case_action_events` carries intent).

---

### API Changes

None. Public `materialize_case_from_review` signature unchanged. New adapter helper exposed via `db.__getattr__`.

---

### Testing

```text
C:\Program Files\Python311\python.exe -m pytest
  tests/test_event_time.py … tests/test_case_policy_e2e.py -q -rs
→ 283 passed, 1 skipped, exit 0
Temporary SQLITE_PATH/DATABASE_URL under %TEMP% (not operational tavidm.db)
git diff --check (repair paths) → exit 0
```

---

### Notes

- No commits, pushes, Mnemosyne writes, training, package installs, or operational DB opens.
- Unrelated dirty-worktree changes outside the listed files were preserved.

---

## 2026-09-07 11:45 — Owner Corrections: Grouping, Settings Groups, Admin Case-Review

### Objective

Implement owner-approved corrections: (1) fully present Illegal Parking and Obstruction under Obstruction of Traffic Flow, (2) configuration-driven grouped violation settings preserving canonical identifiers, (3) active System Administrators may perform all case-review actions while legal-policy approval remains explicit-grant only.

---

### Summary

Settings now render proposed official groups with underlying canonical behaviors; enabling a group expands to canonical rules for persistence and runtime. Parking or obstruction alone materializes under the proposed Obstruction of Traffic Flow category (still unverified). Active admins can confirm/materialize, verify plates, confirm event-time, and attest print; inactive admins and viewers remain denied; admin role still cannot approve legal policy.

---

### Files Modified

#### Created

- `tests/test_owner_corrections_grouping_settings_admin.py`
  - Focused regressions for grouping, settings expansion/mixed state, admin authority.

#### Modified

- `database/sqlite_adapter.py` — `ENFORCEMENT_ROLES` includes `admin`; case-review `can_*` helpers updated.
- `core/case_review_service.py` — authorization docstring/message for case-review roles.
- `core/violation_config.py` — `violation_groups_for_ui`, `expand_group_selection_to_canonical`.
- `config/violation_legal_mappings.json` — grouping notes + Disregarding Traffic Signals `is_grouped_with`.
- `templates/settings.html`, `static/js/settings.js` — grouped toggles with mixed-state preservation.
- `templates/review_queue.html` — proposed category primary, canonical as behavior detail.
- `app.py` — pass/serve `violation_groups`.
- Related tests updated for admin case-review expectations; upload UX switch contract.
- `docs/IMPLEMENTATION_CHECKLIST.md`, this development log.

---

### Testing

```text
C:\Program Files\Python311\python.exe -m pytest
  (established 10-file suite)
  + tests/test_owner_corrections_grouping_settings_admin.py
  + tests/test_violation_config.py
  + tests/test_upload_processing_ux.py::TestToggleSnapshots::test_switch_html_has_active_inactive_and_role
  -q -rs
→ 313 passed, 1 skipped, exit 0
Temp SQLITE_PATH/DATABASE_URL under %TEMP% (not operational tavidm.db)
git diff --check → exit 0
Browser UI pass: not performed (isolated live instance not started).
```

---

### Notes

- No commits, pushes, Mnemosyne writes, training, dependency installs, or operational DB opens.
- Legal mappings remain unverified/flag_only; no promotions or offense-suggestion activation.

---

## 2026-09-07 12:00 — Grouped Switch Mixed-State UI Repair

### Objective

Fix the reproduced regression where `violation_switch.js` `syncSwitchState()` (DOMContentLoaded) overwrote a legacy Mixed grouped toggle to Inactive after `settings.js` had correctly set indeterminate/mixed.

---

### Summary

`syncSwitchState` is now mixed-aware (`indeterminate` / `data-enabled-state=mixed`, with explicit on/off winning over stale aria). `settings.js` initializes Mixed through the shared sync helper and resolves toggles through the same path so wrap/state/ARIA stay consistent. Executable Node regression exercises real init ordering.

---

### Files Modified

#### Created

- `tests/js/test_violation_switch_mixed_state.cjs`
  - Executable JS regression for Mixed survival, Mixed→on/off, untouched save, binary/disabled.

#### Modified

- `static/js/violation_switch.js` — mixed-aware `syncSwitchState` / `isMixedSwitch`.
- `static/js/settings.js` — coordinate Mixed init/change via `TavidmViolationSwitch.sync`.
- `static/css/style.css` — `.switch-mixed` / `.is-mixed` visuals.
- `tests/test_owner_corrections_grouping_settings_admin.py` — asserts mixed-aware switch source.
- `docs/IMPLEMENTATION_CHECKLIST.md`, this development log.

---

### Testing

```text
# Pre-fix reproduction (before repair)
node … → FAIL: Mixed must survive DOMContentLoaded sync; got Inactive

# Post-fix
node tests/js/test_violation_switch_mixed_state.cjs → PASS exit 0
C:\Program Files\Python311\python.exe -m pytest
  TestGroupedSettings + switch HTML contract + test_violation_config.py
→ 19 passed exit 0
git diff --check → exit 0
Browser validation: not performed
```

---

### Notes

- No operational DB, accounts, commits, pushes, dependency installs, or Mnemosyne writes.
- Grouping/auth/materialization/legal-policy logic not reopened.

---

## 2026-09-07 17:46 — Ignore generated output/ and outputs/

### Objective

Exclude generated local review/document package directories from git tracking so `output/` and `outputs/` stop appearing as untracked noise after the reviewed release commits.

---

### Summary

Verified `cursor/phase-1-foundation` matched origin at `68b3d20`. Added `output/` and `outputs/` under the generated-reports section of `.gitignore`, confirmed with `git check-ignore -v` on representative files, then committed only `.gitignore` as `0e6dbfc`. No other files were staged.

---

### Files Modified

#### Modified

- `.gitignore` — ignore `output/` and `outputs/` as generated local review/document packages
- `docs/development_logs/2026-09-07_to_2026-09-09.md` — this entry (uncommitted)

---

### Testing

- `git check-ignore -v` matched dirs and sample files under both trees
- `git status` no longer lists `?? output/` or `?? outputs/`
- Staged/committed only `.gitignore`

---

### Completion Status

✅ Completed (commit `0e6dbfc`; not pushed in this step)

---

## 2026-09-07 17:54 — Track required plate-processing adapter

### Objective

Repair the missing tracked dependency: committed `core/case_review_service.py` imports `core.plate_processing`, but the adapter and its tests were still untracked, so a clean checkout of the release could not import case review.

---

### Summary

Inspected both files (no secrets). Ran the plate adapter suite plus dependent case/review tests against an isolated temp SQLite DB (90 passed). Committed only `core/plate_processing.py` and `tests/test_plate_processing.py` as `f90769d`. Verified in a detached temporary worktree that both files are tracked, `core.case_review_service` imports from the clean checkout (not `D:\tavidm`), and the same 90 tests pass. Worktree removed afterward. Not pushed.

---

### Files Modified

#### Created (tracked)

- `core/plate_processing.py` — FastALPR plate-processing adapter (machine observations only)
- `tests/test_plate_processing.py` — adapter contract tests

#### Modified

- `docs/development_logs/2026-09-07_to_2026-09-09.md` — this entry (uncommitted)

---

### Testing

- Original tree: 90 passed, exit 0 (`SQLITE_PATH`/`DATABASE_URL` temp DB)
- Clean worktree `C:\Users\AMD\AppData\Local\Temp\tavidm_clean_10875e016eca4368a826972f4031f41b`: import_ok; 90 passed, exit 0; then removed

---

### Completion Status

✅ Completed (commit `f90769d`; not pushed)

---
