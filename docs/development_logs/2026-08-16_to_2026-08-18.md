# Development Log

**Project:** TAVIDM — Traffic Violation Detection and Monitoring System

**Development Period:** 2026-08-16 to 2026-08-18

**Repository:** ATAVIDM (https://github.com/tsun2x/ATAVIDM.git)

**Primary Branch:** cursor/phase-1-foundation

**Last Updated:** 2026-08-18 23:50

---

# Overview

This document records all completed development work performed during this three-day development period.

Every log entry represents completed work only.

The documentation should always match the current repository state.

Do not document speculative work, future plans, or partially reverted implementations.

---

# Development Entries

---

## 2026-08-16 18:35 — Vehicle Classification Specification (authoritative, design in progress)

### Objective

- Capture the user-provided Vehicle Classification Specification as the current authoritative design document for TAVIDM.
- Persist the frozen decisions without changing the specification or inventing unfinished classification rules.
- Expected outcome: a canonical spec file agents can load before pipeline work, plus a conflict report against the current codebase.

---

### Summary

Added `docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md` with the hierarchical vehicle-classification decisions (broad class, specific type, PUV operational types, confidence/uncertainty, track persistence, and separation from violation determination). No detector, tracker, violation-engine, or schema code was changed. Existing implementation still uses a flat YOLO `class_label` plus a four-category manuscript map; those conflicts are documented below rather than resolved by silently rewriting the spec.

---

### Files Modified

#### Created

- docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md
  - Canonical Vehicle Classification Specification (ACTIVE / DESIGN IN PROGRESS).

#### Modified

None.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

Documentation update only.

The specification records:

- Hierarchical classification: Broad Vehicle Class → Specific Vehicle Type → optional attributes.
- Required operational classes: Motorcycle; Car / Passenger Vehicle; SUV / Crossover; Van; Truck; Bus.
- PUV operational types that must remain distinct: Jeepney, UV Express / Van, Tricycle, Piaggio (Piaggio retained as an operational label).
- Classification determines rule applicability only; it does not constitute a violation.
- Classification confidence is separate from violation confidence.
- UNKNOWN / UNCERTAIN must be representable.
- Classification should persist on the tracked vehicle; vehicle class is not a unique identity.
- ByteTrack remains responsible for tracking; the Violation Engine consumes classification.
- Operational classification and legal classification stay conceptually separate.
- Later explicit project decisions supersede this document.

---

### Technical Explanation

The specification was stored as project documentation so Cursor, Hermes, and future sessions load the same frozen decisions. Implementation was intentionally not started because several classification details remain design-in-progress, and the spec forbids inventing unspecified mapping rules.

---

### Code Impact

None. Runtime pipeline, APIs, and database schema are unchanged.

---

### Database Changes

None.

---

### API Changes

None.

---

### UI / UX Changes

None.

---

### Dependencies

None.

---

### Testing

Manual Testing: compared the new specification against `core/detection_config.py`, `core/detector.py`, `core/tracker.py`, and `core/violation_engine.py`.

Unit Testing: not run (no code changes).

Integration Testing: not run.

Edge Cases Tested: none in runtime.

Expected Results: spec file present; code unchanged.

Actual Results: spec file created; no Python/schema changes.

Known Issues: see Known Limitations.

Remaining Validation: a Violation Engine specification file is referenced by this document but is not present in `docs/` yet.

---

### Performance Notes

None.

---

### Known Limitations

Current code conflicts with this specification (spec left unchanged; implementation not rewritten in this task):

- `core/detection_config.py` uses a flat `DETECTION_CLASSES` list and maps vehicles to four manuscript categories (`Private Vehicle`, `Public Utility Vehicle`, `Commercial Vehicle`, `Two-Wheeled Vehicle`) via `VEHICLE_CATEGORIES` / `vehicle_category()`.
- Missing operational types in the detector registry: Van, UV Express / Van, Tricycle, Piaggio; no UNKNOWN / UNCERTAIN vehicle type.
- Bus is grouped under `Public Utility Vehicle` in `VEHICLE_CATEGORIES`; the spec treats Bus as a dedicated class distinct from the PUV operational types Jeepney / UV Express / Van / Tricycle / Piaggio.
- Bicycle is grouped with motorcycle as `Two-Wheeled Vehicle`; the spec treats motorcycle as its own broad class and forbids collapsing Tricycle/Piaggio into Motorcycle.
- `core/tracker.py` `TrackHistory` stores a single `class_label` from the first observation; it does not accumulate track-level classification confidence or hierarchical fields (`broad_class`, `vehicle_type`, `is_puv`, `is_motorcycle`, `is_truck`).
- `core/violation_engine.py` consumes YOLO `class_label` / `vehicle_category()` for applicability (for example motorcycle and truck string equality). Classification and violation confidence remain a single detection `confidence` on events.
- Jeepney exists as a YOLO class but is not exposed as `broad_class = PUV` + `vehicle_type = Jeepney` with boolean flags.
- A separate Violation Engine specification document is required by this spec and is not yet in the repository.

---

### Risks

If future implementation maps the new hierarchy onto the existing four-category `vehicle_class` database column without preserving specific types, operational distinctions (Jeepney, Piaggio, Tricycle, UV Express / Van) can be lost. Schema changes were not made in this task.

---

### Next Recommended Steps

- Load or add the complementary Violation Engine specification before changing detection/violation pipeline code.
- When implementation is authorized, report and resolve the conflicts listed above rather than collapsing operational types.
- Do not invent van→UV Express, Piaggio→Motorcycle/Tricycle, or unknown→forced-type mapping rules until those decisions are specified.

---

### Project Progress

#### Completed

✅ Flask monolith with modular `core/` pipeline
✅ YOLOv8m + ByteTrack detection (custom weights preferred; COCO fallback)
✅ Rule engine with currently implemented rules (Obstruction, No Helmet, Counterflow, Truck-Ban Violation, Motorcycle Overloading)
✅ Canonical 11-type violation registry in `core/detection_config.py`
✅ SQLite persistence, review queue, analytics, reports
✅ Role-based auth
✅ Vehicle Classification Specification documented (`docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md`)

#### In Progress

🔄 Vehicle classification hierarchy (specified; not implemented in detector/tracker)
🔄 Remaining canonical violation types still stubbed
🔄 Custom 13-class / PUV-type model coverage vs COCO fallback

#### Remaining

⬜ Implement hierarchical classification fields on tracks without treating class as a violation
⬜ Add Violation Engine specification document when provided
⬜ Align `DETECTION_CLASSES` / `VEHICLE_CATEGORIES` with the specification after explicit implementation authorization
⬜ Preserve UNKNOWN/UNCERTAIN classification in the pipeline

---

## 2026-08-16 18:45 — Violation Engine Specification (authoritative, design in progress)

### Objective

- Capture the user-provided Violation Engine Specification as the current authoritative design document.
- Record the 12-type canonical roster that supersedes the prior fused 11-type list.
- Report conflicts with existing engine code without implementing unspecified TBD rules.

---

### Summary

Added `docs/VIOLATION_ENGINE_SPECIFICATION.md` and linked it from the Vehicle Classification specification. No violation-engine, detector, tracker, UI, or schema code was changed. The specification forbids implementation from a spec drop alone; existing `core/detection_config.py` still lists 11 fused types, and the current rule engine remains zone/dwell-based.

---

### Files Modified

#### Created

- docs/VIOLATION_ENGINE_SPECIFICATION.md
  - Canonical Violation Engine Specification (ACTIVE / DESIGN IN PROGRESS).

#### Modified

- docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md
  - Section 21 now points to `docs/VIOLATION_ENGINE_SPECIFICATION.md`.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

Documentation only. Frozen decisions recorded include:

- Detection alone is not a violation.
- Canonical 12-type roster; Illegal Parking and Illegal Terminal are separate.
- TRUE / FALSE / UNKNOWN context; UNKNOWN is not FALSE.
- Separate detection confidence and per-violation violation confidence.
- Evidence window: 6 seconds before + observed duration + 3 seconds after.
- ALPR after candidate/evidence; unreadable plates do not invalidate the observation.
- Starting (not frozen) heuristics: Illegal Parking ~10s, Obstruction ~3s, Illegal Terminal ~15s with PUV + terminal-like passenger behavior, Counterflow 4s persistent / 2s if already opposing on entry.
- Speed-limit signs excluded from Disregarding Traffic Sign.
- Many thresholds, taxonomies, and applicability matrices remain TBD.

---

### Technical Explanation

The specification was stored as project documentation so later implementation loads both complementary specs. Pipeline code was not rewritten because section 40 forbids implementing from a spec drop and section 38 forbids inventing TBD values.

---

### Code Impact

None at runtime. Future authorized implementation must not treat `core/detection_config.py`'s 11-type `CANONICAL_VIOLATIONS` as the current project roster.

---

### Database Changes

None.

---

### API Changes

None.

---

### UI / UX Changes

None. Per-violation detection/violation confidence controls are specified but not implemented.

---

### Dependencies

None.

---

### Testing

Manual Testing: compared the specification against `core/detection_config.py`, `core/violation_engine.py`, `core/evidence.py`, and `core/tracker.py`.

Unit Testing: not run (no code changes).

Integration Testing: not run.

Edge Cases Tested: none in runtime.

Expected Results: spec file present; engine unchanged.

Actual Results: spec file created; engine unchanged.

Known Issues: see Known Limitations.

Remaining Validation: TBD items listed in specification section 38.

---

### Performance Notes

None.

---

### Known Limitations

Current code conflicts with this specification (spec left unchanged; implementation not rewritten):

- `core/detection_config.py` still defines an 11-type canonical set that fuses Illegal Parking / Illegal Terminal and uses older names (`Substandard Helmet`, `Unauthorized Passengers in Pickup/Truck Cargo Area`).
- Implemented rules are zone/dwell based: fused parking/terminal (10s stop / 30s park), obstruction 10s in active lane (crossing block 3s mapped to Obstruction), counterflow uses global `VIOLATION_PERSISTENCE_SEC` (1.5s) rather than 4s/2s, truck-ban flags trucks in-zone during the window without pre-ban parked-state exemption.
- PUV loading rule treats jeepney+bus as PUV; spec Illegal Terminal requires Jeepney / UV Express / Van / Tricycle / Piaggio plus passenger/terminal behavior, not time-in-zone alone.
- Restricted-lane presence is mapped to pavement-markings; spec forbids automatic marking-crossing violations and leaves marking taxonomy TBD.
- Single global detection-confidence bands (`CONF_AUTO_QUEUE` / `CONF_CAREFUL_REVIEW`); no per-violation detection vs violation confidence pair; `ViolationEvent.confidence` copies detector confidence.
- Evidence is a single annotated snapshot (`core/evidence.py`), not a 6s+duration+3s window; no post-candidate ALPR stage.
- Track history is motion-only; no vehicle-state vocabulary (MOVING / PARKED / UNKNOWN) or three-valued context.
- Motorcycle Overloading currently uses a >2 rider heuristic; spec says occupancy limits are not frozen.
- Low-confidence detections are still queued rather than ignored; spec allows ignore for insufficient evidence.

---

### Risks

If implementation proceeds from existing code names, Illegal Parking and Illegal Terminal can remain fused. Heuristic dwell values in code (30s parking, 10s obstruction, 1.5s persistence) must not be treated as frozen spec values.

---

### Next Recommended Steps

- Do not implement unspecified sign lists, marking taxonomy, occupancy limits, reacquisition scores, or legal confidence numbers.
- When implementation is explicitly requested, split the fused parking/terminal type and introduce separate detection vs violation confidence without inventing TBD defaults as legal facts.
- Await remaining TBD decisions listed in specification section 38.

---

### Project Progress

#### Completed

✅ Flask monolith with modular `core/` pipeline
✅ YOLOv8m + ByteTrack detection (custom weights preferred; COCO fallback)
✅ Rule engine with currently implemented rules (Obstruction, No Helmet, Counterflow, Truck-Ban Violation, Motorcycle Overloading; plus zone-based parking/terminal and related mappings)
✅ SQLite persistence, review queue, analytics, reports
✅ Role-based auth
✅ Vehicle Classification Specification (`docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md`)
✅ Violation Engine Specification (`docs/VIOLATION_ENGINE_SPECIFICATION.md`)

#### In Progress

🔄 Vehicle classification hierarchy (specified; not implemented)
🔄 Violation engine redesign against the 12-type specification (specified; not implemented)
🔄 Remaining specified violation types still incomplete vs the new roster

#### Remaining

⬜ Align `CANONICAL_VIOLATIONS` with the 12-type roster after explicit implementation authorization
⬜ Separate Illegal Parking and Illegal Terminal in engine and schema/UI
⬜ Per-violation detection and violation confidence configuration
⬜ Evidence window 6s + duration + 3s; post-candidate ALPR
⬜ Contextual TRUE/FALSE/UNKNOWN and vehicle-state model (thresholds TBD)

---

## 2026-08-16 19:45 — Violation Engine design update (spec addendum, no code)

### Objective

- Synchronize agent understanding with the 2026-08-16 comprehensive Violation Engine design update.
- Compare the update to the Vehicle Classification spec and the existing Violation Engine spec.
- Record filled TBD items without silently resolving roster or legal conflicts, and without changing production code.

---

### Summary

Added `docs/VIOLATION_ENGINE_DESIGN_UPDATE_2026-08-16.md` as an addendum. Pointed the two living specifications at that file. Did not merge Illegal Parking and Illegal Terminal despite fused wording in the update’s §23. No detector, tracker, database, or violation-engine code was modified.

---

### Files Modified

#### Created

- docs/VIOLATION_ENGINE_DESIGN_UPDATE_2026-08-16.md
  - Design addendum plus open-conflict register.

#### Modified

- docs/VIOLATION_ENGINE_SPECIFICATION.md
  - Section 38 TBD list and section 42 status now reference the addendum and the unresolved roster conflict.
- docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md
  - Section 21 status now references the addendum.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

Documentation only. Newly recorded design direction includes: context-aware pipeline; PARKED vs mere stationarity; hazard lights not a legal exemption; max two on-motorcycle occupants; helmet categories for rider and rear passenger; mirror PRESENT/ABSENT/UNKNOWN; initial 5-second lost-track window; STOP and speed-limit excluded; initial sign scope feeding Disregarding Traffic Sign; double/single/solid+broken pavement working rules; ALPR after candidate; evidence 6s+duration+3s restated as buffers not thresholds.

Open conflict B.1 (roster fusion vs 12 separate types) is documented, not resolved.

---

### Technical Explanation

An addendum was used instead of rewriting the base Violation Engine spec’s §4 roster so a later fused list would not silently supersede an earlier explicit 12-type decision. Production code was left unchanged per the update’s required action.

---

### Code Impact

None.

---

### Database Changes

None.

---

### API Changes

None.

---

### UI / UX Changes

None.

---

### Dependencies

None.

---

### Testing

Manual Testing: compared the design update against both specification documents and noted conflicts in the addendum.

Unit Testing: not run (no code changes).

Integration Testing: not run.

Edge Cases Tested: none in runtime.

Expected Results: addendum present; production code unchanged.

Actual Results: addendum present; production code unchanged.

Known Issues: roster fusion conflict remains owner-owned.

Remaining Validation: owner confirmation of conflict B.1 and remaining TBD items.

---

### Performance Notes

None.

---

### Known Limitations

The addendum does not freeze numeric state thresholds, reacquisition scoring, parking/obstruction/terminal persistence, per-sign triggers, cargo geometry, or confidence defaults. Pickup/dump/flatbed are named for cargo-area applicability but are not in the vehicle-classification hierarchy.

---

### Risks

If a later implementer treats update §23 as authoritative without reading the conflict register, Illegal Parking and Illegal Terminal may be fused incorrectly.

---

### Next Recommended Steps

- Project owner should explicitly confirm whether the canonical roster stays 12 separate types or is fused.
- Do not implement violation logic until that roster decision and remaining TBD items needed by the chosen rule are specified.
- Map pickup/dump/flatbed to the vehicle-classification hierarchy when authorized.

---

### Project Progress

#### Completed

✅ Flask monolith with modular `core/` pipeline
✅ YOLOv8m + ByteTrack detection (custom weights preferred; COCO fallback)
✅ Existing rule engine (not yet aligned to the living specifications)
✅ Vehicle Classification Specification
✅ Violation Engine Specification
✅ Violation Engine design-update addendum (2026-08-16)

#### In Progress

🔄 Living specifications (design in progress; professor/panel review expected)
🔄 Unresolved canonical-roster fusion conflict

#### Remaining

⬜ Owner decision on Illegal Parking vs Illegal Terminal (separate vs fused)
⬜ Implementation remains unauthorized from this update

---

## 2026-08-16 19:50 — Confirm Illegal Parking and Illegal Terminal as separate canonical types

### Objective

- Record the owner decision that Illegal Parking and Illegal Terminal are separate canonical violations.
- Recalculate the canonical count from the existing authoritative roster without adding, removing, renaming, or merging other types.
- Update living specification documents only. Do not modify production code or invent numerical thresholds.

---

### Summary

Updated `docs/VIOLATION_ENGINE_SPECIFICATION.md` §4, §18, §20, and §42 so Illegal Parking and Illegal Terminal are explicitly separate rules and separate records that may share upstream context. Marked design-update conflict B.1 as resolved. Canonical count remains **12**. Production detector/engine/database code was not changed and may still use a fused label.

---

### Files Modified

#### Created

None.

#### Modified

- docs/VIOLATION_ENGINE_SPECIFICATION.md
  - Roster confirmed as 12 separate types; parking vs terminal distinction restated; status notes production code is not yet aligned.
- docs/VIOLATION_ENGINE_DESIGN_UPDATE_2026-08-16.md
  - Conflict B.1 marked RESOLVED; fused §23 wording superseded.
- docs/VEHICLE_CLASSIFICATION_SPECIFICATION.md
  - Status line notes 12 types with parking and terminal separate.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

Specification update only. The existing 12-type list was preserved. No new violation was invented. Parking/terminal persistence numbers were not added or frozen.

---

### Technical Explanation

The owner decision closes the earlier fusion conflict. Historical development-log entries that described the conflict as open were left unchanged (append-only). Production `VIOLATION_ILLEGAL_PARKING_TERMINAL` remains an implementation gap, not a spec change.

---

### Code Impact

None at runtime.

---

### Database Changes

None.

---

### API Changes

None.

---

### UI / UX Changes

None.

---

### Dependencies

None.

---

### Testing

Manual Testing: searched documentation for remaining fused canonical-category wording in living specs.

Unit Testing: not run (no code changes).

Integration Testing: not run.

Edge Cases Tested: none in runtime.

Expected Results: specs say 12 separate types; code unchanged.

Actual Results: specs updated; `core/detection_config.py` still contains fused production label.

Known Issues: production code not aligned.

Remaining Validation: implementation when authorized.

---

### Performance Notes

None.

---

### Known Limitations

`core/detection_config.py` and `core/violation_engine.py` still use `Illegal Parking / Illegal Terminal` as a single production string. That is documented as not-yet-authorized implementation debt.

---

### Risks

None beyond the existing code/spec mismatch until implementation is authorized.

---

### Next Recommended Steps

- Do not implement until explicitly requested.
- When implementation is authorized, split the fused production label into two canonical types without inventing thresholds.

---

### Project Progress

#### Completed

✅ Owner confirmation: Illegal Parking and Illegal Terminal are separate
✅ Canonical roster count = 12 in living specifications

#### In Progress

🔄 Living specifications (design in progress)
🔄 Production code still uses a fused parking/terminal label

#### Remaining

⬜ Implementation of separate parking and terminal rules (not authorized)

---

## 2026-08-18 10:30 — Canonical 12-type violation registry and global toggle system

### Objective

- Restore the frozen 12-type canonical violation scope (Illegal Parking and Illegal Terminal separate).
- Wire a truthful end-to-end global violation enable/disable mechanism.
- Preserve honest implementation status (implemented / partial / planned / model-dependent).
- Do not invent detection logic for planned violations.

---

### Summary

Corrected `core/detection_config.py` to define exactly 12 canonical violations with separate `Illegal Parking` and `Illegal Terminal` identifiers. The fused `Illegal Parking / Illegal Terminal` string is retained only as a legacy alias via `LEGACY_FUSED_PARKING_TERMINAL`. Split parking/terminal rule functions in `core/violation_engine.py`, added `core/violation_config.py` for global toggle persistence in `system_settings.enabled_violations`, wired `load_enabled_violations()` into uploaded-video and live-stream processing, extended Settings UI/API for violation toggles with status badges, updated analytics canonicalization, and added/updated tests.

---

### Files Modified

#### Created

- core/violation_config.py
  - Global enabled-violations load/save/validate and UI catalog metadata.
- tests/test_violation_config.py
  - Registry, legacy mapping, persistence, and validation tests.

#### Modified

- core/detection_config.py
  - 12-type canonical registry; IMPLEMENTED/PARTIAL/PLANNED/MODEL_DEPENDENT/TOGGLEABLE sets; legacy mappings.
- core/violation_engine.py
  - `check_illegal_parking()` and `check_illegal_terminal()` split; `evaluate_detection_rules()` filters by enabled set.
- core/video_processor.py
  - Passes `enabled_violations=load_enabled_violations()` into rule engine.
- core/live_stream.py
  - Same enabled-violations wiring as video processor.
- core/analytics.py
  - Canonicalizes violation counts for dashboard KPIs and breakdowns.
- app.py
  - Violations/Reports filters use all 12 canonical types; Settings/API expose violation catalog and toggles.
- templates/settings.html
  - Violation Rules section with status badges and toggleable checkboxes.
- static/js/settings.js
  - Saves `enabled_violations` with rule parameters.
- tests/test_violation_engine.py
  - Updated for 12 canonical types; split parking/terminal tests; enabled-set filtering tests.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

**Canonical registry:** `CANONICAL_VIOLATIONS` now lists 12 frozen types. `IMPLEMENTED_VIOLATIONS` remains 5 core executable rules. `PARTIAL_VIOLATIONS` covers parking, terminal, and pavement-markings proxies. `PLANNED_VIOLATIONS` covers four types without rule functions.

**Toggle architecture:** Settings UI → `POST /api/settings` `{ enabled_violations: [...] }` → `system_settings.enabled_violations` JSON → `load_enabled_violations()` in `video_processor.py` / `live_stream.py` → `evaluate_detection_rules(enabled_violations=...)`. Global toggles only (no per-video/per-camera schema).

**Parking vs terminal:** `check_illegal_parking()` uses `no_parking` zone dwell; `check_illegal_terminal()` uses `loading_unloading` zone dwell with PUV class filter. Both are partial implementations, not full spec behavior.

**Default runtime behavior preserved:** `DEFAULT_ENABLED_VIOLATIONS` = prior 5 core rules when setting is unset.

---

### UI / UX Changes

- Settings page shows all 12 canonical violations with Implemented / Model-dependent / Partial / Planned badges.
- Planned violations are visible but checkbox-disabled (cannot be enabled).

---

### Dependencies

None.

---

### Testing

Unit Testing:

```bash
.\venv\Scripts\python.exe -m pytest tests/test_violation_engine.py tests/test_violation_config.py -v
.\venv\Scripts\python.exe -m pytest tests/ -v
```

Expected Results: all violation registry, filtering, persistence, and split parking/terminal tests pass.

Actual Results: 27 violation tests passed; full suite 53 passed.

Integration Testing: not run against live Flask UI in this session.

Edge Cases Tested: empty enabled set suppresses all rules; planned violations rejected on save; legacy fused string maps to Illegal Parking for analytics.

Known Issues: none in test suite.

Remaining Validation: manual Settings UI save/reload; live stream with partial rules enabled.

---

### Performance Notes

None — configuration load is one DB read per processing session.

---

### Known Limitations

- Four canonical violations remain planned (no rule functions).
- Parking, terminal, and pavement-markings rules are partial zone-dwell proxies.
- No Helmet requires custom YOLO weights with helmet class to trigger in production.
- Historical DB records may still contain legacy violation strings; analytics canonicalizes counts but raw report rows are unchanged.

---

### Risks

Low — default enabled set preserves prior 5-rule runtime behavior.

---

### Next Recommended Steps

- Manual verification of Settings toggle save/reload in browser.
- When authorized, implement planned violation rule functions without changing canonical scope.
- Consider report summary canonicalization if historical legacy strings should aggregate under canonical names in exports.

---

### Project Progress

#### Completed

✅ 12-type canonical violation registry (Parking and Terminal separate)
✅ Global violation toggle persistence and processing propagation
✅ Settings UI violation catalog with honest implementation status
✅ Split parking/terminal rule functions
✅ Analytics canonicalization for dashboard counts
✅ Tests for registry, toggles, and filtering (53/53 passing)

#### In Progress

🔄 Full spec implementation for partial/planned violation types (not in scope for this task)

#### Remaining

⬜ Substandard Helmet, Disregarding Traffic Sign, No Side Mirror, Cargo Passengers rule logic
⬜ Full contextual parking/terminal/pavement-markings detection per specification
⬜ Per-video or per-camera violation profiles (not currently required by schema)

---

## 2026-08-18 23:50 — Sequential queue: queued flag + stoppable worker

### Objective

- Fix `POST /api/videos/<id>/process` reporting `queued: false` for a second video that is actually waiting behind a running job.
- Stop Windows pytest teardown `PermissionError` caused by the background worker holding the temp SQLite file.
- Expected outcome: `queued` means "will not start immediately"; tests can join the worker before the temp DB directory is deleted.

---

### Summary

The worker pops a job before running it, so queue position alone cannot describe "waiting." A `_running_video_id` slot flag now drives `queued`. The single worker is stoppable (`stop_processing_worker`) so tests join it before deleting the temp database. Full suite: 78 passed.

---

### Files Modified

#### Created

None.

#### Modified

- `app.py`
  - Track `_running_video_id`; compute `queued` as slot busy or queue position > 1; add `stop_processing_worker()` and shutdown-aware worker loop.
- `tests/test_processing.py`
  - Autouse fixture depends on `test_db_path` and stops the worker around each test; mock `process_video` on API tests that do not need inference.
- `tests/conftest.py`
  - `client` fixture calls `stop_processing_worker()` after yield.
- `docs/development_logs/2026-08-16_to_2026-08-18.md`
  - This entry.

#### Renamed

None.

#### Deleted

None.

---

### Detailed Changes

- `_enqueue_processing` now returns whether the job must wait: another video occupies `_running_video_id`, or this job is not first in `_process_queue`.
- Worker sets `_running_video_id` when it pops a job and clears it in `finally`.
- Worker loop honors `_worker_shutdown` (wait timeout 0.5s plus `notify_all` on stop).
- `_ensure_worker` restarts a thread if the previous one is not alive (after test shutdown).
- Processing tests tear down the worker before the temp SQLite directory is removed.

---

### Technical Explanation

`queued` must not use "index in `_process_queue`" after the worker has already popped the running job. The running slot is a separate piece of state. Test isolation requires a joinable worker because a daemon thread can keep a Windows SQLite file lock across `TemporaryDirectory` cleanup.

---

### Code Impact

- Process API `queued` is true whenever the new job will not start immediately, including when it is the only remaining queue item behind a running video.
- Tests that POST `/process` should mock `process_video` and rely on `stop_processing_worker()`.

---

### Database Changes

None.

---

### API Changes

Modified Endpoints

- `POST /api/videos/<id>/process` — `queued` is true if another video currently occupies the inference slot, not only if queue position > 1.

---

### UI / UX Changes

None in templates. Live Monitor already shows "queued" from the process response flag; that flag is now correct.

---

### Dependencies

None.

---

### Testing

Unit Testing: `python -m pytest tests/ -v --tb=short` — 78 passed.

Edge Cases Tested: second video queued while first is in-flight; same-video 409; process-status error after worker finishes.

Known Issues: none observed in this run (no Windows PermissionError).

---

### Performance Notes

None. Sequential single-worker inference slot unchanged.

---

### Known Limitations

- `stop_processing_worker` joins the current job (up to timeout) rather than cancelling mid-inference.
- Live streams still use global `load_enabled_violations()`.

---

### Risks

Low. Production never calls `stop_processing_worker`; the worker remains a single daemon thread.

---

### Next Recommended Steps

- Manual Live Monitor click-through: upload → annotate → confirm violations → queue two videos → confirm second shows queued.
- Document `MAX_UPLOAD_MB` / `UPLOAD_DISK_HEADROOM_MB` in README and `.env.example` if not already done.

---

### Project Progress

#### Completed

✅ Sequential one-slot processing queue with accurate `queued` flag
✅ Stoppable worker for test isolation

#### Currently In Progress

None for this task.

#### Remaining Major Milestones

⏳ Manual E2E of pre-process confirmation + multi-video queue in the browser
⏳ ALPR remains out of scope (`plate_status='not_attempted'`)

---

### Developer Notes

`queued = (_running_video_id is not None and != this video) or position > 1`. Do not infer waiting from queue length after pop.

---

### Completion Status

Status:

✅ Completed

---

---

## Entry — 2026-08-18 16:03 (UTC+8) — Senior Review: Lifecycle Cleanup

**Author:** Cursor (senior reviewer)
**Type:** Code review / verification

### Summary

Independent senior review of Hermes' lifecycle cleanup implementation covering two issues:

1. **Orphaned queued/running recovery** — `recover_orphaned_processing()` in `database/sqlite_adapter.py` gained a second pass that fails any `processing_runs` still in `('queued', 'running')` regardless of `videos.status`, catching runs that were waiting in the in-memory FIFO when the app crashed.
2. **Logging for `start_processing_run` failures** — `app.py` now uses `logger.exception()` instead of bare `except: pass` around `db.start_processing_run(run_id)`.

### Files Reviewed (not modified by this review)

| File | Change by Hermes |
|------|-----------------|
| `database/sqlite_adapter.py` | Second-pass recovery logic (lines 521–536) |
| `app.py` | `import logging`, `logger`, exception logging (lines 11, 30, 760–767) |
| `tests/test_processing.py` | Rewritten `test_recovers_orphaned_queued_run`, added `test_recovers_orphaned_running_run_without_processing_video` |

### Verification

- Full test suite: **83 passed** in 38.47s (independently run by Cursor)
- No schema changes, no queue redesign, no scope expansion

### Review Verdict

**Approved — ready to commit.**

---

# End of Entry
